<h1><%= @place.name %></h1>
<div class="booyah-box col-10 offset-1">
  <h1>Photos</h1>
  <% @place.photos.each do |photo| %>
    <div class="booyah-box col-10 offset-1">
      <%= image_tag photo.picture_url.to_s, class:"image_preview" %>
      <%= photo.caption %>
    </div>
  <% end %>
</div>